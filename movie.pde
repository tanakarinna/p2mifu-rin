void movie(){
// currentImage = puropo;
// if((mouseX >= 701 && mouseX <= 701 + 197 && mouseY >= 528 && mouseY <= 528 + 197)&&(scene==4)){
//       if (currentImage==saisei) {
//       currentImage = puropo;  // img1 -> img2 に切り替え
//     } else {
//       currentImage = saisei;  // img2 -> img1 に切り替え
//     }//再生と停止を切り替える
//   }
    // mousePressed();
    image(currentImage,0,0,1600,1200);
}